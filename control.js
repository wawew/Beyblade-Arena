// control
    // let radSpeed;
    // if(rightPressed) {
    //     playerOne.dx += 0.2;
    //     radSpeed = Math.sqrt((playerOne.dx ** 2) + (playerOne.dy ** 2))
    //     if (radSpeed > maxSpeed && playerOne.dx > maxSpeed) {
    //         playerOne.dx -= 0.2;
    //     } else if (radSpeed > maxSpeed) {
    //         if (playerOne.dy > 0) {
    //             playerOne.dy -= 0.2
    //         } else {
    //             playerOne.dy += 0.2
    //         }
    //     }
    // }
    // else if(leftPressed) {
    //     playerOne.dx -= 0.2;
    //     radSpeed = Math.sqrt((playerOne.dx ** 2) + (playerOne.dy ** 2))
    //     if (radSpeed > maxSpeed && playerOne.dx < maxSpeed) {
    //         playerOne.dx += 0.2;
    //     } else if (radSpeed > maxSpeed) {
    //         if (playerOne.dy > 0) {
    //             playerOne.dy -= 0.2
    //         } else {
    //             playerOne.dy += 0.2
    //         }
    //     }
    // }
    // else if(upPressed) {
    //     playerOne.dy -= 0.2;
    //     radSpeed = Math.sqrt((playerOne.dx ** 2) + (playerOne.dy ** 2))
    //     if (radSpeed > maxSpeed && playerOne.dy < maxSpeed) {
    //         playerOne.dy += 0.2;
    //     } else if (radSpeed > maxSpeed) {
    //         if (playerOne.dx > 0) {
    //             playerOne.dx -= 0.2
    //         } else {
    //             playerOne.dx += 0.2
    //         }
    //     }
    // }
    // else if(downPressed) {
    //     playerOne.dy += 0.2;
    //     radSpeed = Math.sqrt((playerOne.dx ** 2) + (playerOne.dy ** 2))
    //     if (radSpeed > maxSpeed && playerOne.dy > maxSpeed) {
    //         playerOne.dy -= 0.2;
    //     } else if (radSpeed > maxSpeed) {
    //         if (playerOne.dx > 0) {
    //             playerOne.dx -= 0.2
    //         } else {
    //             playerOne.dx += 0.2
    //         }
    //     }
    // }
    // end control
    // update on ball collision
    // let collision = ballCollision(playerOne, playerTwo);
    // let speedOneX = collision.updatedSpeedOneX;
    // let speedOneY = collision.updatedSpeedOneY;
    // playerOne.setSpeed(collision.updatedSpeedOneX, collision.updatedSpeedOneY);
    // playerTwo.setSpeed(collision.updatedSpeedTwoX, collision.updatedSpeedTwoY);
    // update on wall collision
    